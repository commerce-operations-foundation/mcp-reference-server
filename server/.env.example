# Server Configuration
LOG_LEVEL=info               # debug, info, warn, error

# Adapter Configuration
ADAPTER_TYPE=built-in            # built-in, npm, local
ADAPTER_NAME=mock                # For built-in: mock, pipe17, etc.
ADAPTER_PACKAGE=                 # For npm: package name
ADAPTER_PATH=                    # For local: file path
ADAPTER_EXPORT=                  # Optional: export name (default: 'default')
ADAPTER_CONFIG=                  # Optional: JSON config for adapter options

# Mock Adapter Settings (when ADAPTER_NAME=mock)
# Add to ADAPTER_CONFIG as JSON:
# ADAPTER_CONFIG='{"fixedLatency":100,"errorRate":0.05}'

# Pipe17 Adapter Settings (when ADAPTER_NAME=pipe17)
# Add to ADAPTER_CONFIG as JSON:
# ADAPTER_CONFIG='{"apiUrl":"https://api.pipe17.com","apiKey":"your-key","workspaceId":"your-workspace"}'

# NPM Adapter Example
# ADAPTER_TYPE=npm
# ADAPTER_PACKAGE=@company/oms-adapter
# ADAPTER_EXPORT=OMSAdapter
# ADAPTER_CONFIG='{"apiKey":"key123"}'

# Local Adapter Example
# ADAPTER_TYPE=local
# ADAPTER_PATH=/path/to/adapter.js
# ADAPTER_EXPORT=MyAdapter
# ADAPTER_CONFIG='{"endpoint":"http://localhost:3000"}'
