/**
 * Mock Data
 * Realistic mock data generation and storage for testing
 */
import { Customer, Fulfillment, InventoryItem, Order, Product, ProductVariant, Return } from '../../schemas/index.js';
export declare class MockData {
    readonly orders: Map<string, Order>;
    readonly products: Map<string, Product>;
    readonly productAliases: Map<string, string>;
    readonly productVariants: Map<string, ProductVariant>;
    readonly customers: Map<string, Customer>;
    readonly inventory: Map<string, InventoryItem>;
    readonly returns: Map<string, Return>;
    readonly fulfillments: Map<string, Fulfillment>;
    constructor();
    /**
     * Generate realistic sample data
     */
    private generateSampleData;
    /**
     * Generate sample products
     */
    private generateSampleProducts;
    /**
     * Generate sample customers
     */
    private generateSampleCustomers;
    /**
     * Generate sample inventory
     */
    private generateSampleInventory;
    /**
     * Generate sample orders
     */
    private generateSampleOrders;
    /**
     * Generate sample returns
     */
    private generateSampleReturns;
    /**
     * Get data size for health checks
     */
    getSize(): {
        orders: number;
        products: number;
        productVariants: number;
        customers: number;
        inventory: number;
        returns: number;
        fulfillments: number;
    };
    /**
     * Clear all data (for testing)
     */
    clear(): void;
    /**
     * Reset to sample data (for testing)
     */
    reset(): void;
}
//# sourceMappingURL=mock-data.d.ts.map