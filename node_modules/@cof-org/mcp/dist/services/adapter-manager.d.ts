/**
 * Adapter Manager
 * Manages Fulfillment adapter lifecycle including initialization, health checks, and cleanup
 */
import { IFulfillmentAdapter, AdapterConfig, HealthStatus, AdapterCapabilities } from '../types/index.js';
export declare class AdapterManager {
    private healthCheckIntervalMs;
    private adapter;
    private config;
    private isInitialized;
    private initializationPromise;
    private healthCheckInterval;
    private lastHealthCheck;
    private lastHealthStatus;
    constructor(healthCheckIntervalMs?: number);
    /**
     * Initialize adapter with configuration
     */
    initialize(config: AdapterConfig): Promise<void>;
    private doInitialize;
    /**
     * Get current adapter instance
     */
    getAdapter(): IFulfillmentAdapter;
    /**
     * Check if adapter is initialized
     */
    isReady(): boolean;
    /**
     * Get adapter capabilities
     */
    getCapabilities(): Promise<AdapterCapabilities | null>;
    /**
     * Check adapter health
     */
    checkHealth(): Promise<HealthStatus>;
    /**
     * Get last health status
     */
    getLastHealthStatus(): {
        status: HealthStatus | null;
        checkedAt: Date | null;
    };
    /**
     * Start health monitoring
     */
    private startHealthMonitoring;
    /**
     * Stop health monitoring
     */
    private stopHealthMonitoring;
    /**
     * Update adapter configuration
     */
    updateConfig(config: AdapterConfig): Promise<void>;
    /**
     * Clean up adapter resources
     */
    cleanup(): Promise<void>;
    /**
     * Get adapter information (sync version - capabilities not included)
     */
    getInfo(): {
        type: string | null;
        initialized: boolean;
        lastHealthCheck: Date | null;
        lastHealthStatus: string | null;
    };
    /**
     * Get adapter information with capabilities (async version)
     */
    getInfoWithCapabilities(): Promise<{
        type: string | null;
        initialized: boolean;
        lastHealthCheck: Date | null;
        lastHealthStatus: string | null;
        capabilities: AdapterCapabilities | null;
    }>;
}
//# sourceMappingURL=adapter-manager.d.ts.map