/**
 * Error Handler
 * Centralized error handling and operation execution with metrics
 */
interface ServiceMetrics {
    requestCount: number;
    errorCount: number;
    averageResponseTime: number;
    lastOperation: string | null;
}
export declare class ErrorHandler {
    private metrics;
    /**
     * Execute operation with error handling, retry, and metrics
     */
    executeOperation<T>(operationName: string, operation: () => Promise<T>, retryOptions?: {
        maxRetries: number;
        initialDelay: number;
    }): Promise<T>;
    /**
     * Map adapter errors to Fulfillment errors
     */
    private mapAdapterError;
    /**
     * Update response time metrics
     */
    private updateResponseTime;
    /**
     * Get current metrics
     */
    getMetrics(): ServiceMetrics;
    /**
     * Reset metrics
     */
    resetMetrics(): void;
}
export {};
//# sourceMappingURL=error-handler.d.ts.map