/**
 * Service Orchestrator
 * Main facade that coordinates all service components and maintains backward compatibility
 */
import type { CreateSalesOrderInput, CancelOrderInput, UpdateOrderInput, FulfillOrderInput, CreateReturnInput, GetOrdersInput, GetCustomersInput, GetProductsInput, GetProductVariantsInput, GetInventoryInput, GetFulfillmentsInput, GetReturnsInput } from '../schemas/tool-inputs/index.js';
import { AdapterConfig, HealthStatus, OrderResult, ReturnResult, FulfillmentToolResult } from '../types/adapter.js';
import { Customer, Fulfillment, InventoryItem, Order, Product, ProductVariant, Return } from '../schemas/index.js';
/**
 * ServiceOrchestrator provides a unified interface to all Fulfillment operations
 */
export declare class ServiceOrchestrator {
    private adapterManager;
    private healthMonitor;
    private errorHandler;
    constructor();
    /**
     * Initialize the service with adapter configuration
     */
    initialize(config: AdapterConfig): Promise<void>;
    /**
     * Check if service is initialized
     */
    isInitialized(): boolean;
    /**
     * Update adapter configuration
     */
    updateAdapterConfig(config: AdapterConfig): Promise<void>;
    createSalesOrder(input: CreateSalesOrderInput): Promise<OrderResult>;
    cancelOrder(input: CancelOrderInput): Promise<OrderResult>;
    updateOrder(input: UpdateOrderInput): Promise<OrderResult>;
    fulfillOrder(input: FulfillOrderInput): Promise<FulfillmentToolResult<{
        fulfillment: Fulfillment;
    }>>;
    createReturn(input: CreateReturnInput): Promise<ReturnResult>;
    getInventory(input: GetInventoryInput): Promise<FulfillmentToolResult<{
        inventory: InventoryItem[];
    }>>;
    getOrders(input: GetOrdersInput): Promise<FulfillmentToolResult<{
        orders: Order[];
    }>>;
    getProducts(input: GetProductsInput): Promise<FulfillmentToolResult<{
        products: Product[];
    }>>;
    getProductVariants(input: GetProductVariantsInput): Promise<FulfillmentToolResult<{
        productVariants: ProductVariant[];
    }>>;
    getCustomers(input: GetCustomersInput): Promise<FulfillmentToolResult<{
        customers: Customer[];
    }>>;
    getFulfillments(input: GetFulfillmentsInput): Promise<FulfillmentToolResult<{
        fulfillments: Fulfillment[];
    }>>;
    getReturns(input: GetReturnsInput): Promise<FulfillmentToolResult<{
        returns: Return[];
    }>>;
    checkHealth(): Promise<HealthStatus>;
    getMetrics(): object;
    private ensureInitialized;
    private recordSuccess;
    private recordFailure;
    /**
     * Clean up resources
     */
    cleanup(): Promise<void>;
}
//# sourceMappingURL=service-orchestrator.d.ts.map