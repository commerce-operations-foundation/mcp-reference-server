import { z } from 'zod';
import { TemporalPaginationSchema } from './shared/filters.js';
export const GetCustomersInputSchema = TemporalPaginationSchema.extend({
    ids: z.array(z.string()).optional().describe('Unique customer ID in the Fulfillment System'),
    emails: z.array(z.email()).optional().describe('Customer email address'),
});
//# sourceMappingURL=get-customers.js.map