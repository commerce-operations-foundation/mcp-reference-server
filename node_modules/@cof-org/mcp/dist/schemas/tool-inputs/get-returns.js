import { z } from 'zod';
import { TemporalPaginationSchema } from './shared/filters.js';
export const GetReturnsInputSchema = z
    .object({
    ids: z.array(z.string()).describe('Internal return IDs'),
    orderIds: z.array(z.string()).describe('Order IDs to find returns for'),
    returnNumbers: z.array(z.string()).describe('Return numbers (customer-facing identifiers)'),
    statuses: z.array(z.string()).describe('Return statuses'),
    outcomes: z.array(z.string()).describe('Return outcomes (refund/exchange)'),
})
    .partial()
    .extend(TemporalPaginationSchema.shape);
//# sourceMappingURL=get-returns.js.map