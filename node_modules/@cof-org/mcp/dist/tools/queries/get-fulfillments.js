import { BaseTool } from '../base-tool.js';
import { GetFulfillmentsInputSchema } from '../../schemas/tool-inputs/index.js';
import { toJsonSchema } from '../../schemas/utils/schema-util.js';
export class GetFulfillments extends BaseTool {
    name = 'get-fulfillments';
    description = 'Retrieves shipping details and tracking information for customer updates or delivery verification. Use when: answering "where is my order" inquiries, providing tracking updates, verifying delivery completion, or investigating shipping issues. Searches by shipment ID, order ID, or carrier tracking number.';
    inputSchema = toJsonSchema(GetFulfillmentsInputSchema);
    async execute(input) {
        return this.serviceLayer.getFulfillments(input);
    }
}
//# sourceMappingURL=get-fulfillments.js.map