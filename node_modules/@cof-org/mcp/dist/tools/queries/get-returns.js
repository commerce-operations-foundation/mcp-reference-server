import { BaseTool } from '../base-tool.js';
import { toJsonSchema } from '../../schemas/utils/schema-util.js';
import { GetReturnsInputSchema } from '../../schemas/tool-inputs/index.js';
export class GetReturnsTool extends BaseTool {
    name = 'get-returns';
    description = 'Retrieves return records with filtering by return ID, order ID, return number, status, or outcome. Use when: tracking return status, finding returns for specific orders, or generating return reports.';
    inputSchema = toJsonSchema(GetReturnsInputSchema);
    async execute(input) {
        return this.serviceLayer.getReturns(input);
    }
}
//# sourceMappingURL=get-returns.js.map