import { BaseTool } from '../base-tool.js';
import { toJsonSchema } from '../../schemas/utils/schema-util.js';
import { GetProductVariantsInputSchema } from '../../schemas/tool-inputs/index.js';
export class GetProductVariantsTool extends BaseTool {
    name = 'get-product-variants';
    description = 'Retrieves detailed SKU-level product variant information, including option selections, pricing, and inventory tracking flags. Use when you need variant-specific data for merchandising, order validation, or inventory operations. Supports lookups by variant ID, SKU, or parent product ID.';
    inputSchema = toJsonSchema(GetProductVariantsInputSchema);
    async execute(input) {
        return this.serviceLayer.getProductVariants(input);
    }
}
//# sourceMappingURL=get-product-variants.js.map